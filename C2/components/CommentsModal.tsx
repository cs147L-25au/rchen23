/**
 * Deprecated: thread UI is `app/postComments/[eventId].tsx`.
 * Stub only—keeps Metro from failing if an old broken copy of this file exists.
 */
import React from "react";

export default function CommentsModal() {
  return null;
}
